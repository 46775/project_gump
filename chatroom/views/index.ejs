<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Chat App</title>
  <style>
    body {
      height: 100vh;
      overflow: hidden;
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #1d1d1d;
      background-size: cover;
    }

    .main {
      background-color: rgba(32, 32, 32, 0.916);
      padding: 20px;
      text-align: center;
      border-radius: 10px;
      width: 80%;
      max-width: 600px;
      margin: 50px auto;
      box-shadow: 0px 0px 20px rgba(255, 255, 255, 0.249);
      color: whitesmoke;
    }

    #room-container {
      max-height: 300px;
      overflow-y: auto;
      padding: 10px;
      border: 2px solid #cfcfcf;
      border-radius: 5px;
      margin-bottom: 20px;
      overflow: scroll;
      overflow-x: hidden;
    }

    #room-container div {
      margin-bottom: 10px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 10px;
      background-color: #f7f7f7;
      border-radius: 5px;
      color: black;
      font-weight: bold;
    }

    #room-container a {
      text-decoration: none;
      color: #007bff;
      padding: 8px 15px;
    }
    
    #room-container a:hover {
      transition: background-color 0.3s ease;
      background-color: #0056b3;
      color: #fff;
      border-radius: 25px;
    }

    form {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    h2{
      text-shadow: 0 0 5px rgba(32, 209, 228, 0.826);
      letter-spacing: 2px;
    }
    form input[type="text"] {
      border: 2px solid #107cff;
      border-radius: 5px;
      padding: 10px;
      width: 100%;
      max-width: 300px;
      margin-bottom: 10px;
      transition: border-color 0.3s ease;
    }

     form button {
      padding: 10px 20px;
      border: none;
      border-radius: 25px;
      background-color: transparent;
      color: #acacac;
      cursor: pointer;
      transition:  0.6s ease;
    }

    form button:hover {
      background-color: #0056b3;
      font-weight: bold;
    }
  </style>
</head>

<body background="first.jpg">
  <div class="main">
    <h2>Welcome to Chat Room</h2>
    <div id="room-container">
      <% Object.keys(rooms).forEach(room=> { %>
        <div>
          <span>
            <%= room %>
          </span>
          <a href="/<%= room %>">Join</a>
        </div>
        <% }) %>
    </div>
    <form action="/room" method="POST">
      <input name="room" type="text" placeholder="Enter room name" required>
      <button type="submit">Create Room</button>
    </form>
  </div>
</body>

</html>